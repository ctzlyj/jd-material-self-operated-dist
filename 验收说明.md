# 2026.09.17.1 受保护版验收

- 私有源码和干净安装各469项；受保护候选和真实干净安装各469项、223次隔离核心调用通过。根79项、服务/浏览器751项及传输15项通过。
- 从原公开受保护`.9`升级通过，保留本地任务，冲突暂存，拒绝覆盖维护人私有安装。Windows系统加密凭据曾用模拟值跨Python进程验证，不保存维护人真实Key。
- 授权服务只追加新版自营核心，保留原自营、POP及另一产品现行发布；授权名单、审计及访问策略核验不变，不恢复或重建数据库。
- 隔离HTTP覆盖新版自营、`.9`、旧默认自营各8个及POP6个核心函数，并使用1+10个合成SPU及完整SKU验证。
- 线上使用真实维护人身份及实际安装客户端验证；结果见`DELIVERY_VERIFICATION.json`。未开通资格时正确返回`ERP_NOT_ALLOWED`，不为验收临时授权，不冒用同事身份。拒绝验证不是获准同事的正向生产验收。
- 私有源码下载被拒绝。同事电脑、其他OS原生密钥库及所有同事真实业务尚未逐机验收。
- Gemini Pro/Flash默认并发2/1是保守配置，不证明最大稳定并发或生产提速。接口接受、回读匹配、素材审核通过和信息分刷新分别记录。
