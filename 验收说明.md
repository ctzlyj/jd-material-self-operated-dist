# 2026.09.16.9 受保护分发验收

## 已验证

- 完整受保护客户端427项测试通过，执行219次本地核心计算；不依赖维护人本机ERP。
- 服务和浏览器751项、根79项、浏览器传输15项、离线身份隔离3项通过。新增版本路由和客户端请求测试均先失败后修复。
- 实际构建服务的隔离HTTP验收：新自营核心`.9`、旧自营核心`.29`、POP核心`.1`分别执行全部8/8/6个受保护函数，各覆盖1+10合成SPU和2+20完整SKU，校验幂等回执。
- 旧自营和POP默认核心逐文件SHA保持不变；新核心以显式版本路由追加。版本与请求哈希绑定，未知/非法版本不回退。
- 真实构建入口可启动，读取版本映射并通过健康检查。既有服务的数据目录、域名、访问策略及授权名单不因升级改变。
- 公开包只含远程替身，不含私有核心、生产账本、授权名单或凭据。安装器保留独有文件、本地修改及待合并冲突，不覆盖私有安装。

## 验证边界

维护人本人的受保护自营及POP资格未启用，本次不临时授权、不冒用同事ERP。线上确认真实身份和拒绝边界，正向8/8/6函数验证来自隔离HTTP服务，不宣称已完成新版线上获准ERP正向验收。实际部署、域名、名单前后校验结果见`DELIVERY_VERIFICATION.json`。

未运行真实商品扫描、模型生成、上传、绑定或短标题任务；未恢复任何历史维护。合成用例通过不等于同事生产维护、审核通过、信息分刷新或生产提速。各同事仍需自己的登录、已开通的自营Skill资格和安全模型配置。
