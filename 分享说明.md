# 自营同事一键维护

客户端版本：2026.09.15.2。受保护远程核心版本：2026.09.14.29，完整核心模块兼容性经 AST 比较验证，源 SHA 位于 `jd-material-self-operated/PROTECTED_BUILD.json`。

## 同事怎么用

1. 保存这个完整文件夹，不要只拿 `SKILL.md`。保留 `jd-material-self-operated`、`webcli-browser-runtime`、`tools` 和 `DISTRIBUTION.json`。
2. 在 Codex 中提供这个文件夹的位置，复制 `开始使用.txt` 里的提示词。安装、依赖检查、首次建任务和执行命令交给 Codex。
3. 首次只需配合必要登录或本地隐藏输入。登录和授权具备后，无需提供旧任务、店名、Excel、SKU列表或再次确认写入。

## 维护人发出前

在 https://material-auth.space.jd.com 为同事的真实 ERP 开通“自营 Skill”资格。POP Skill、插件资格不通用；本包不自动添加名单，也不借用维护人的身份。

只发送本文件夹。不要发送私有源码仓库、服务端代码、旧混合 Skill、模型 Key、登录凭据、生产账本或商品缓存。

## 运行条件与结果

需要 Windows、Python 3.12+、Node.js 22.15+、京东内网、登录的 HiOffice、自营业务登录和 Browser Bridge，以及本人获准使用的模型安全配置。Codex 负责检查和准备工具，必要的扫码/登录仍由本人完成，不绕过权限控制。

入口自动按本人 ERP 建立新计划并执行 1/10/剩余全量；中断后沿同一目录续跑。旧任务必须沿原账本恢复，不能为“重新开始”丢掉排除和未知回执。有效图片复用，单商品失败保留完整清单；全局鉴权、额度、范围或未知写入异常会安全中断。

结束查看任务目录中的 `first-use-summary.json` 和 `全流程失败与待核实SKU清单.csv`。商品信息分受平台审核及刷新影响；接口接受与回读匹配不等于审核通过或信息分已刷新。
